# Test requirements for ADK Agent Evaluation
# Install with: pip install -r tests/requirements.txt

# Core testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ADK evaluation (should already be installed if you have google-adk)
google-adk>=0.1.0

# For better test output
pytest-html>=4.0.0
pytest-cov>=4.1.0
